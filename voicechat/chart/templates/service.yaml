apiVersion: v1
kind: Service
metadata:
  name: voicechat
spec:
  type: {{ .Values.service.type }}
  selector:
    app: voicechat
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      nodePort: {{ .Values.service.nodePort }}
